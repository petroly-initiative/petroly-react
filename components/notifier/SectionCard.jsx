
/**
 * TODO
 * @props
 * - isCheckbox: boolean prop to reuse the same component on both the modal and off-canvas display
 * - checkListener: a function that bubbles up the checked selection number to update state
 * ? state
 * @checked boolean state
 * ! handle language and themes
 * 
 * @returns a card that dispaly section number, building number, status, days, instructor name, time
 */
function SectionCard(props){

    return <></>
}

export default SectionCard;